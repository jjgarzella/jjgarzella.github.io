<?xml version="1.0" encoding="UTF-8"?>

<rss version="2.0"
  xmlns:content="http://purl.org/rss/1.0/modules/content/"
  xmlns:dc="http://purl.org/dc/elements/1.1/"
  xmlns:media="http://search.yahoo.com/mrss/"
  xmlns:atom="http://www.w3.org/2005/Atom"
  xmlns:georss="http://www.georss.org/georss">

  <channel>
    <title>
      <![CDATA[  Jack J. Garzella  ]]>
    </title>
    <link> jjgarzella.github.io </link>
    <description>
      <![CDATA[  Jack J. Garzella&#39;s academic website and blog  ]]>
    </description>
    <atom:link
      href="jjgarzella.github.io/feed.xml"
      rel="self"
      type="application/rss+xml" />


<item>
  <title>
    <![CDATA[  How to Install Julia and Oscar like a Pro  ]]>
  </title>
  <link> jjgarzella.github.io/blog/how-to-install-julia/index.html </link>
  <guid> jjgarzella.github.io/blog/how-to-install-julia/index.html </guid>
  <description>
    <![CDATA[  Overview of the Julia/Oscar toolchain  ]]>
  </description>  
  
  <content:encoded>
    <![CDATA[  <h1 id="how_to_install_julia_and_oscar_like_a_pro">How to Install Julia and Oscar like a Pro</h1>
<p>In this post, we&#39;ll discuss how to install <a href="https://www.oscar-system.org/">Oscar</a> on your personal computer. I&#39;ll assume a very little familiarity with programming, and none with any of the specific concepts or tools we&#39;ll discuss. Instead of doing the fastest possible thing to get started, I&#39;d like to discuss <em>why</em> we&#39;re doing what we&#39;re doing along the way. This will lead us into a discussion of recent advances in software development over the past 20-25 years. </p>
<h2 id="what_even_is_a_computer_algebra_system">What even is a computer algebra system?</h2>
<p>I want to clear up one subtlety in terminology. In order to do mathematical research with programming, you need &#40;at least&#41; two things:</p>
<ul>
<li><p>A <em>programming language</em>, i.e. a set of commands that can be used to write computer programs. Things like if-statements, while-loops, for-loops, lists, and so on are part of the programming language. This is Turing-complete and a priori has nothing to do with any specific use cases, such as mathematical programming.</p>
</li>
<li><p>A <em>standard library</em> or <em>framework</em>, i.e. a set of functions which are provided out of the box for the user. This may include standard coding exercises like sorting a list, or very math-specific things like computing the class group of a number field.</p>
</li>
</ul>
<p>This distinction can be confusing at times, because many computer algebra systems provide both a programming language and a framework, releasing them with the same name. One example of this is <a href="https://macaulay2.com/">Macaulay2</a>. On the other hand, we will use the <a href="https://julialang.org/">Julia programming language</a>, with <a href="https://www.oscar-system.org/">Oscar</a> as our framework.</p>
<p>Thus, to install Oscar it is enough to install Julia, and install various Julia packages &#40;including Oscar itself&#41;.</p>
<h2 id="installing_julia">Installing Julia</h2>
<h3 id="windows">Windows</h3>
<ul>
<li><p>It turns out that Oscar doesn&#39;t actually support Windows. This is ok though, because we can use Windows Subsystem for Linux &#40;WSL&#41;, see the <a href="https://www.oscar-system.org/install/win/">instructions</a> on Oscar&#39;s website. </p>
</li>
<li><p>I highly recommend installing the <a href="https://apps.microsoft.com/detail/9n0dx20hk701?hl&#61;en-US&amp;gl&#61;US">Windows terminal</a>. Among other things, it&#39;ll let you use math symbols like Ω or ≤ in your code. </p>
</li>
</ul>
<h3 id="mac">Mac</h3>
<ul>
<li><p>If you&#39;re on a Mac, you&#39;ll need to install the Apple developer tools, see the <a href="https://www.oscar-system.org/install/mac/">instructions</a> on Oscar&#39;s website.</p>
</li>
</ul>
<h2 id="step_1_version_manager_install_julia">Step 1: Version manager &#40;Install Julia&#41;</h2>
<p>Any programming language periodically have updates which add functionality, fix bugs, increase performance, and so on. It&#39;s nice to be able to use the latest version of your language, since it&#39;s likely to work better and faster. On the other hand, updating can be a pain, and even worse, sometimes you update and then it breaks your code.</p>
<p>Software engineers have dealt with thse issue for decades, and in the past 10-20 years the community seems to have converged on a solution. A <em>version manager</em> is a program that manages multiple versions of the language installed at once. For example, a while back Oscar was broken with Julia version 1.10 for some reason on my desktop computer. I used Julia version 1.8 for some of my research code. Eventually, whatever bug was causing the problem got fixed and I started using the latest version, which by that time was 1.11.</p>
<p>Julia&#39;s version mananger is called <a href="https://github.com/JuliaLang/juliaup"><code>juliaup</code></a>, which is a reference to <a href="https://rustup.rs/"><code>rustup</code></a>. <a href="https://www.rust-lang.org/">Rust</a> is a programming language that got really popular among software developers in the 2010s, and it has rave reviews. For example, it consistently wins the &quot;most loved&quot; technologies on the <a href="https://stackoverflow.blog/2020/06/05/why-the-developers-who-use-rust-love-it-so-much/">Stack Overflow Developer Survey</a>. One of the reasons for Rust&#39;s popularity is the tools that surround it, including <code>rustup</code>, the version manager for Rust. As such, Rust has had a huge influence on software engineering since the mid-2010&#39;s. In particular, <code>juliaup</code> is designed to work in a very analogous way to <code>rustup</code>.</p>
<p>Rust has not yet been used that much for mathematics, but maybe this will change in the future: there are already people doing stuff with <a href="https://docs.rs/cygv/latest/cygv/">Gromov-Witten invariants</a> and <a href="https://github.com/SpectralSequences/sseq">stable homotopy theory</a>. </p>
<p>You can installed <code>juliaup</code> using the <a href="https://github.com/JuliaLang/juliaup">instructions in <code>juliaup</code>&#39;s README</a> &#40;if you&#39;re on Windows, make sure to use the &quot;Linux&quot; instructions in your WSL terminal&#41;. </p>
<p>To check if you&#39;ve properly installed <code>juliaup</code>, you can type <code>julia</code> into the command line. You see a Julia prompt &#40;if it doesn&#39;t work, you might need to restart the terminal window&#41;. This is called the Julia <em>REPL</em>, which stands for <em>R</em>ead <em>E</em>valuate <em>P</em>rint <em>L</em>oop. You type commands into the REPL, and Julia prints out the output of the command. To verify that the REPL is working properly, you can try doing basic arithmetic:</p>
<pre><code class="language-julia">julia&gt; 2&#43;2
4</code></pre>
<h2 id="step_2_package_manager_install_oscar">Step 2: Package manager &#40;Install Oscar&#41;</h2>
<p>Once we&#39;ve installed Julia, Oscar can simply be installed as a Julia package. A <em>package</em> is, roughly, a self-contained collection of code that&#39;s meant to be used by others, and a <em>package manager</em> is a program that downloads packages from the internet and takes care of installation and configuration. </p>
<p>Julia&#39;s package manager, known as <code>Pkg</code>, is modeled closely after <code>cargo</code>, the package manager for Rust. <code>cargo</code> also dates to the mid-2010&#39;s and has gotten rave reviews. Most modern package managers since then have either improved on or just copied the general design strategy of <code>cargo</code>.</p>
<p>To use Julia&#39;s package manager, open a Julia REPL &#40;i.e. type <code>julia</code> into the command line if you haven&#39;t already&#41; and press the end square bracket <code>&#93;</code>. The prompt should turn blue. Then type <code>add Oscar</code> and click enter. It&#39;ll take a while installing a bunch of things.</p>
<h2 id="bonus_step_25_if_you_want_to_use_your_gpu">Bonus Step 2.5: If you want to use your GPU</h2>
<p>Julia&#39;s GPU libraries will work on most computers that were bought post-2020. The first thing you&#39;ll need to know is what brand of GPU you have. Your choices are Nvidia, AMD, Intel, or Apple. You should be able to find this in the system settings. Once you know the brand, the Julia library you install is listed here:</p>
<table><tr><th align="right">Vendor</th><th align="right">Julia Library Name</th></tr><tr><td align="right">Nvidia</td><td align="right">CUDA</td></tr><tr><td align="right">AMD</td><td align="right">ROCm</td></tr><tr><td align="right">Intel</td><td align="right">oneAPI</td></tr><tr><td align="right">Apple</td><td align="right">Metal</td></tr></table>
<p>I&#39;ll do the example of Nvidia. In this case, you&#39;ll go into package mode and run <code>add CUDA</code>. Then, regardless of what brand you&#39;re using, run <code>add KernelAbstractions</code> and <code>add AcceleratedKernels</code>. </p>
<h2 id="step_3_ide_install_vs_code_and_plugins">Step 3: IDE &#40;Install VS Code and Plugins&#41;</h2>
<p>An IDE &#40;Integrated Development Environment&#41; is basically a souped-up code editor with extra features that help you write code. You don&#39;t technically <em>need</em> an IDE to write code, but it can be a much nicer experience. Over the past few years, <strong>VS Code</strong> &#40;short for Visual Studo Code&#41; has become a very popular IDE. If you don&#39;t want to use VS Code, you can use any other IDE that you&#39;re familiar with, but here we&#39;ll focus on VS code. </p>
<p>First, you&#39;ll want to download and install VS Code from the <a href="https://code.visualstudio.com/">VS Code website</a>. Then, open VS Code.  If you&#39;re on a Mac, you can now install all of the plugins you want– click the extensions icon on the left side, type <code>julia</code> into the search box. Find the &quot;Julia Language Support&quot; plugin, and click install.</p>
<h3 id="brief_digression_for_windows_users">Brief digression for Windows users</h3>
<p>If you&#39;re using Windows, you&#39;ll have to connect your VS Code to WSL. First, install the WSL extention by clicking the extensions icon on the left, and typing in <code>wsl</code> into the search bar. Find the WSL extension and click Install.</p>
<p>Next, go to the search bar at the very top of VS Code, and type in <code>&gt;wsl</code>, and among the search options should be <code>WSL: Connect to WSL</code>.  Choose this option, and VS Code will reload itself.</p>
<p>Now, you can install the Julia plugin &#40;and any other plugins you may want&#41;. Click the extensions icon on the left again, type <code>julia</code> into the search bar, and click the <code>Install in WSL</code> option.</p>
<h2 id="trying_it_out">Trying it out</h2>
<p>If you&#39;ve got everything installed, go ahead and try it out&#33; Type the following into the REPL &#40;or just copy and paste the whole thing, including the <code>julia&gt;</code> prompts&#41;, and see if you get the same results:</p>
<pre><code class="language-julia">julia&gt; using Oscarjulia&gt; R, &#40;x,y,z&#41; &#61; ZZ&#91;:x,:y,:z&#93;
&#40;Multivariate polynomial ring in 3 variables over ZZ, ZZMPolyRingElem&#91;x, y, z&#93;&#41;julia&gt; I &#61; ideal&#40;R, x*y &#43; z^2&#41;
Ideal generated by
  x*y &#43; z^2julia&gt; Q, map &#61; quo&#40;R,I&#41;
&#40;Quotient of multivariate polynomial ring by ideal &#40;x*y &#43; z^2&#41;, Map: R -&gt; Q&#41;julia&gt; Q&#40;y^2*x&#41; &#61;&#61; Q&#40;-y*z^2&#41;
true</code></pre>
<h2 id="tldr">TL;DR</h2>
<p>Text that is in <code>monospace font</code> means that you should type the code into the terminal and press enter.</p>
<h3 id="windows__2">Windows</h3>
<ol>
<li><p>Download Ubuntu from the Windows app store</p>
</li>
<li><p>Download Windows Terminal from the Windows app store</p>
</li>
<li><p>Open Windows Terminal &#40;in Ubuntu&#41;</p>
</li>
<li><p><code>curl -fsSL https://install.julialang.org | sh</code></p>
</li>
<li><p><code>julia</code> &#40;open a Julia REPL&#41;</p>
</li>
<li><p>Type <code>&#93;</code> to enter package mode</p>
</li>
<li><p><code>add Oscar</code></p>
</li>
<li><p>Type backspace to exit package mode </p>
</li>
<li><p><code>using Oscar</code></p>
</li>
<li><p>Download and install VS Code</p>
</li>
<li><p>Install the WSL plugin</p>
</li>
<li><p>Run the command <code>&gt;WSL: Connect to WSL</code> in VS Code</p>
</li>
<li><p>Install the Julia plugin in WSL</p>
</li>
</ol>
<h3 id="mac__2">Mac</h3>
<ol>
<li><p>Launch a terminal</p>
</li>
<li><p><code>xcode-select --install</code>, then confirm </p>
</li>
<li><p><code>curl -fsSL https://install.julialang.org | sh</code></p>
</li>
<li><p><code>julia</code> &#40;open a Julia REPL&#41;</p>
</li>
<li><p>Type <code>&#93;</code> to enter package mode</p>
</li>
<li><p><code>add Oscar</code></p>
</li>
<li><p>Type backspace to exit package mode </p>
</li>
<li><p><code>using Oscar</code></p>
</li>
<li><p>Download and install VS Code</p>
</li>
<li><p>Install the Julia plugin</p>
</li>
</ol>
 ]]>
  </content:encoded>
    
  <pubDate>Thu, 19 Feb 2026 00:00:00 +0000</pubDate>  
  
  
  <atom:author>
    <atom:name>Jack J. Garzella</atom:name>
  </atom:author>
        
</item>
</channel></rss>